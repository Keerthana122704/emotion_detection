//Check whether a tree is binary or not

class Solution {
    boolean isBST(Node root) {
        return isBSTUtil(root, Long.MIN_VALUE, Long.MAX_VALUE);
    }

    boolean isBSTUtil(Node node, long min, long max) {
        if (node == null) return true;

        if (node.data <= min || node.data >= max) return false;

        return isBSTUtil(node.left, min, node.data) &&
               isBSTUtil(node.right, node.data, max);
    }
}

Given the root of a binary tree. Your task is to return the left view of the binary tree. 
The left view of a binary tree is the set of nodes visible when the tree is viewed from the left side.

Note: If the tree is empty, return an empty list.

nodes = [1, 2, 3, N, N, 4, N, N, 5, N, N]
Output: [1, 2, 4, 5]
/*
class Node {
    int data;
    Node left, right;

    Node(int val) {
        this.data = val;
        this.left = null;
        this.right = null;
    }
}
*/

class Solution {
    public ArrayList<Integer> leftView(Node root) {
        // code here
        ArrayList<Integer> res=new ArrayList<>();
        Queue<Node> q=new ArrayDeque<>();
        
        q.add(root);
        while(! q.isEmpty()){
            res.add(q.peek().data);
            int sizee=q.size();
            for(int i=0; i<sizee;i++){
                Node curr=q.poll();
                if(curr.left!=null) q.add(curr.left);
                if(curr.right!=null) q.add(curr.right);
                
            }
        }return res;
    }
}
//-------------------------  OR   ------------------------
// class Solution {
//     public ArrayList<Integer> leftView(Node root) {
//         ArrayList<Integer> res = new ArrayList<>();
//         leftViewUtil(root, 1, 0, res);
//         return res;
//     }

//     private void leftViewUtil(Node node, int level, int maxLevel, ArrayList<Integer> res) {
//         if (node == null) return;

//         // First node of this level
//         if (maxLevel < level) {
//             res.add(node.data);
//             maxLevel= level;
//         }

//         // Recurse left first, then right
//         leftViewUtil(node.left, level + 1, maxLevel, res);
//if all my left is null, but right is visible in the view.......... 
//         leftViewUtil(node.right, level + 1, maxLevel, res);
//     }
// }
